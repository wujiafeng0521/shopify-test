<div>
    <div class="profile">
        <img src="{{ section.settings.user_img |  img_url: '300*300' }}" />
        <h3>{{ section.settings.user_name }}</h3>
        <p>{{ section.settings.user_bio }}</p>
    </div>
</div>

{% schema  %}  
    {
        "name":"Profile",
        "settings": [
            {
                "id":"user_name",
                "type": "text",
                "label": "User name"
            },{
                "type": "image_picker",
                "id": "user_img",
                "label": "User photo"
            },{
                "type": "richtext",
                "id": "user_bio",
                "label": "User bio"
            }
        ],
        "presets": [
            {
                "category":"Profile",
                "name": "User profile"
            }
        ]
    }
{% endschema %}